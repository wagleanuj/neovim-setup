-- LazyVim loads this automatically (in addition to its own keymaps).
-- Only bind keys LazyVim leaves free. Plugin-specific keys live in their plugin specs.

-- NOTE: C-h/j/k/l window navigation is now provided by vim-tmux-navigator
-- (see lua/plugins/navigation.lua), so it seamlessly crosses into tmux panes.
-- Add other free-key maps here as needed.
